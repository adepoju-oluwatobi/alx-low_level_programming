#include <stdio.h>

/**
 * add - function that sums up two integers
 * a: first integer parameter
 * b: second integer parameter
 *
 * Return: addition of a and b
 */

int add(int a, int b)
{
	return (a + b);
}
