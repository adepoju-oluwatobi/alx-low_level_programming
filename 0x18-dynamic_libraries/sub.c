#include <stdio.h>

/**
 * sub - function that sums up two integers
 * a: first integer parameter
 * b: second integer parameter
 *
 * Return: subtraction of a and b
 */

int sub(int a, int b)
{
        return (a - b);
}

